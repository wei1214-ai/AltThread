package com.example.myapplicationkoG.editor

/**
 * Tools planned for the editor. Part 1 has them disabled in the UI;
 * selection is wired so Part 2 can plug in real behaviour.
 */
enum class EditorTool {
    NONE,
    DYE,
    CUT,
    DISTRESS,
    PATCH,
    STITCH,
    FABRIC
}
