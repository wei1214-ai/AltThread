# Pin the Gradle daemon JVM to Android Studio's bundled JBR (exactly 21.0.10).
# The IDE-hosted JRE is 21.0.11 and lacks jlink.exe, which breaks AGP's
# JdkImageTransform. The exact version makes JBR the only valid candidate.
jvm.version=21.0.10
